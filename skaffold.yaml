apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/orgtesttags/nodetest:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/orgtesttags/nodetest:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: nodetest
        chartPath: charts/nodetest
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/orgtesttags/nodetest'
          image.tag: '{{.DIGEST_HEX}}'
